#include <stdio.h>

int main() {
    int x,y,z;
    float avg;
    scanf("%d %d %d", &x,&y,&z);
    avg = (x+y+z)/3;
    if(avg=0){
        printf("Average: %.2f", avg);
    }
    else {
        printf();
    }
    return 0;
}